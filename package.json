{
  "name": "social-networking-dinosaur",
  "version": "1.0.0",
  "description": "A modern social networking platform with live streaming and gaming",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ..",
    "start": "concurrently \"npm start --prefix server\" \"npm start --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "build": "npm run build --prefix client",
    "deploy": "npm run build && npm start --prefix server"
  },
  "keywords": ["social", "network", "streaming", "gaming", "dinosaur"],
  "author": "Anointing Kolapo Olalere",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}